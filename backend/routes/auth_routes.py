from fastapi import APIRouter

router = APIRouter()

# Note: Authentication endpoints are currently implemented in app.py
# TODO: Move auth endpoints from app.py to this router
# The following routes are stubs - actual implementation is in app.py

# Auth endpoints are handled in app.py:
# - /api/auth/login
# - /api/auth/signup
# - /api/auth/demo-login
# - /api/auth/verify
# - /api/auth/resend-verification
# - /api/auth/logout
